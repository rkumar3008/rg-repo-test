trigger:
- main
- feature/*

stages:
- stage: 
  displayName: Validate_Plan
  pool: rk-agent-pool
  jobs:
  - job: 
    displayName: Validate
    steps: 
    - task: TerraformInstaller@1
      displayName: Terraform_cli_install
      inputs:
        terraformVersion: 'latest'
    - task: TerraformTask@5
      displayName: Terraform_init
      inputs:
        provider: 'azurerm'
        command: 'init'
        backendServiceArm: 'ranveer'
        backendAzureRmResourceGroupName: 'rk-backend'
        backendAzureRmStorageAccountName: 'rk320stg'
        backendAzureRmContainerName: 'rk-container'
        backendAzureRmKey: 'state.tfstate'

    - task: TerraformTask@5
      displayName: Validate
      inputs:
        provider: 'azurerm'
        command: 'validate'
    - task: TerraformTask@5
      inputs:
        provider: 'azurerm'
        command: 'plan'
        environmentServiceNameAzureRM: 'ranveer'


